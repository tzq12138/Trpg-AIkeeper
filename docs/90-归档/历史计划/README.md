# 历史计划归档

本目录收纳原仓库根目录 `plan/plan*.md` 的历史计划。归档目的只是让计划文档能从 `docs/` 内统一检索，不改变历史计划正文。

## 使用口径

- 新计划应写入 `docs/30-DeepSeek任务包/` 或后续明确的 `docs/` 子目录。
- 不要重新在仓库根目录创建 `plan/`。
- 如果确实需要临时草稿，优先放入 `docs/90-归档/草稿/` 或当前任务约定的文档目录。
