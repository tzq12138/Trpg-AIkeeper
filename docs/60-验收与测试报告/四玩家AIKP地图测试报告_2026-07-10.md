# AI-Keeper 四玩家、AI KP 与地图测试报告

## 结论

- 最终四人循环房间：`b2736bf4`；剧本：`向火独行.pdf`（`136694d7`）。
- 四个角色均成功导入、入房、Ready、开局，并在同一回合完成四条行动结算。
- 战役以显式验收结局 `victory / 成功逃脱` 结束；循环没有触发手动 AI fallback。
- 运行中未再出现“周围暂时没有新的变化”占位叙事，战报的每条 KP 叙事只显示一次。

## 最终实测证据

- P1：`侦查`，掷骰/目标 `15/65`，困难成功。
- P2：`话术`，掷骰/目标 `38/52`，普通成功。
- P3：`侦查`，掷骰/目标 `18/25`，普通成功。
- P4：`心理学`，掷骰/目标 `5/32`，极难成功。
- 地图状态为 `confirmed`，含 `9` 个节点、`11` 条边、`4` 个玩家初始位置和 `1` 个已探索起点。
- 浏览器人工路径已覆盖 Host 全图、玩家迷雾图、相邻节点移动提交及回合结算后的地图刷新。

## 修复项

- MCP 会话失效时自动重连并重试，适配服务或模型切换后的旧会话失效。
- 原子认领行动，避免自动结算和手动 AI 回合并发时重复裁决。
- 确认地图开局时为所有玩家放置起点并揭示起点。
- CY20 XLSX 在公式缓存缺失时由基础值、职业点和兴趣点重算技能。
- AI 技能别名按角色卡标准技能归一化；未知技能安全降级为对话，避免 `0` 阈值检定。
- 战报按 `actionId` 去除事务摘要与公开叙事的重复显示。
- 前端技能分配允许消耗最后不足 `5` 点的余数。

## 验证命令

- `python -m pytest tests/server -q`：`457 passed`。
- `cd src/client && npm run test -- --run`：`9 passed`。
- `cd src/client && npm run build`：通过。
- `python scripts/run_multiplayer_loop.py --api-base http://127.0.0.1:3002 --ensure-accounts --scenario-id 136694d7 --turn-script scripts/loop_actions_yhdx.json --output-dir docs/loop_runs`：通过。

## 产物与切换说明

- 最终原始捕获：`docs/loop_runs/20260710-073915-b2736bf4.json`。
- 最终战报：`docs/loop_runs/20260710-073915-b2736bf4.md`。
- 当前本地实测使用可用的 MCP/DeepSeek 提供方；GPT-5.4 切换代码已具备，但本机未配置 OpenAI 密钥，未对真实 GPT-5.4 发起调用验证。
- 更换多模态 API 后，应先检查 `/api/health` 中 `provider_health.mcp`，再复跑上述 loop；脚本支持用 `--api-base` 指向新后端实例。
