# 权威 CoC7 RAG 与房间管理验收记录

实际执行日期：2026-08-14
对应方案：`docs/superpowers/plans/2026-08-13-authoritative-coc7-rag-and-room-pagination.md`

## 结论

权威 CoC7 核心规则书已按页导入、审核并发布；运行时规则检索只使用合格版本。旧的本地测试规则材料没有物理删除，但其依赖房间已经隔离，不能再进入游戏。管理员页面已支持房间分页、当前页全选和显式确认的批量删除。

## 正式来源与实际导入

- 来源文件：`data/test_assets/六类黄金样本/规则书/COC7th核心规则书v1.2.1.pdf`
- SHA-256：`22F5F56B7A0989CBDED695D39C7D5EDDDDD809CFC9D2C47E4CF4C5D7EDEA6815`
- 物理页数：380
- 实际导入的规则版本：`d7488077-0bb5-5683-bbab-d20945d6fe0d`
- 审计结果：376 页可检索、4 页归档非检索、0 页待人工审核；发布门禁为 `ready`，版本状态为 `published`，运行资格为 `true`。

每一个可检索分块都保留来源文档、页级来源部分、真实 PDF 页码和页内范围。再次导入会校验 1–380 页的页记录及对应页级来源部分；缺页或缺少页级来源部分时不会把损坏来源当成成功导入或可发布版本。

## 旧规则与旧房间

- 已执行管理员显式隔离接口；实际响应为 `retired_rule_versions=0`、`retired_rooms=70`。
- 旧测试规则、分块和关联记录仍保留，供管理员审计；没有物理删除规则素材。
- 被隔离房间显示“规则源已失效，无法打开”，游戏、玩家、房主、重连、导出、地图、RAG 及后台结算入口均会返回稳定的 `rule_source_retired` 状态。
- 管理员详情、审计和删除仍可访问这些房间；管理员删除时会同时清理房间局部裁定，避免外键阻断。

## 浏览器验收

在本地 `http://127.0.0.1:5173` 以 `admin` 身份完成：

1. `/rag-test` 显示正式来源文件名、SHA-256、380 页统计、发布/运行状态和 `ready` 审计门禁；历史测试索引明确标识为“仅供维护”。
2. 四道黄金问题均通过，并返回正式规则的真实页码 citation：
   - 困难成功和极难成功怎么判定；
   - 理智检定失败会怎样；
   - 幸运值如何回复；
   - 孤注一掷失败会发生什么。
3. 虚构问题“量子护盾如何充能”返回空结果，不再以“充能”等单一偶然词命中魔法值条目。
4. `/admin` 房间页显示 73 个房间、当前第 1 页 10 条、10/30/50 每页档位和 8 页总分页；当前页全选后恰好选择 10 个房间。验收未删除任何已有房间。

## 自动化验证

- `python -m pytest tests/server/test_rag_search.py tests/server/test_rag_router.py tests/server/test_rag_security.py -q`：54 passed。
- `npm run test -- RagTestPage.test.tsx AdminDashboard.test.tsx`：7 passed。
- `npm run build`：TypeScript 检查和 Vite 生产构建通过。
- `git diff --check`：通过；仅有 Windows 工作区既有的 LF/CRLF 转换提示。

此前各实现提交已分别完成页级导入、运行资格、旧房间守卫、后台结算竞态、分页及批量删除的定向回归；本记录中的浏览器结果是在真实导入和显式隔离后重新验证。

## 运行边界

- 隔离动作不会迁移或复活旧房间；它们保留给管理员审计或分页删除。
- 已经建立并保持连接的实时 WebSocket 不会由隔离动作主动广播踢出；新的连接和后续受守卫的操作会被拒绝。这是本轮明确保留的边界，若未来需要“隔离瞬时断开所有在线连接”，应单独设计连接回收机制。
- 正式规则原文、来源指纹、页码、检索得分和局部临时裁定继续限于管理员审计路径；普通房主和玩家不能读取维护索引或完整规则原文。
