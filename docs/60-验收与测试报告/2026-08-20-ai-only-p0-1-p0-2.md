# AI-Only P0-1/P0-2 第一交付包

日期：2026-08-20  
分支：`codex/20260813-rag-corpus-audit`

## 已完成

- 新增根目录唯一验收规范 [`AI_ONLY_ACCEPTANCE_SPEC.md`](../../AI_ONLY_ACCEPTANCE_SPEC.md)。
- 定义 `session_mode=ai_only` 的角色边界、自主决策矩阵、兼容状态机、KP 八组行为规则、运行时最小字段、Glass Rain Golden Run 和发布指标。
- 新增 `AiOnlyResolutionPolicy`，把 Host 异常边界集中到 `src/server/engine/host_autonomy.py`。
- 草稿确认前拒绝遗留的 `host_exception` 路由，不创建 action 或状态事件。
- 纯 AI 房间不创建玩家 Host review 请求；历史 review 和 action exception 也不能由 Host 端点列出或处理。

## 自动化证据

```text
python -m pytest \
  tests/server/test_host_autonomy.py \
  tests/server/test_action_reviews_v2.py \
  tests/server/test_action_drafts_v2.py -q
83 passed, 1 warning
```

新增回归先以 Red 复现旧行为，再 Green 验证：

- 遗留 AI-only Host 异常草稿：旧行为 200，现为 409，且 action/status event 数量为 0；
- AI-only 玩家申诉：旧行为创建 Host review，现为稳定 `ai_only_review_not_host_routed`；
- AI-only 历史 review：Host 查询返回同一稳定错误；
- 非纯 AI Host review、Host exception 和原有行动草稿回归仍通过。

另已通过：

- `python -m compileall -q`（本次修改的四个后端模块）；
- `git diff --check`。

## 尚未宣称完成的内容

- 玩家申诉的自动重新解释/基于原始 `RollReceipt` 的重算尚未实现；当前先 fail-closed，不能回退到 Host。
- P0-3（Prompt 深审）、P0-4（剧本运行时字段）、P0-6A（统一 Decision Trace）、P0-5（真实 Glass Rain Golden Run）和 P0-6B（批量仿真）尚未开始。
- 本轮没有重新执行真实浏览器整场跑团，因此没有把自动化通过写成产品发布通过。

工作树中已有的 `CLAUDE.md` 修改及其他未跟踪文档未纳入本交付包。

