# User 用户与权限系统 — 文档更新回执

**日期:** 2026-07-06
**审查范围:** `功能列表.md` / `PRD.md` / `DeepSeek计划.md` 最新版本
**实现基线:** User-0 至 User-5 全部批次已完成

---

## 一、文档变化概要

更新版新增约 +40% 内容，集中在三块：**生产安全基线**（6 项缺口识别）、**凭证边界对照表**（7 种凭证的准确语义）、**长期演进方向**（token 生命周期、ws_ticket、RAG 裁剪）。

---

## 二、逐项审查结论

### ✅ 已闭环

| FR 编号 | 要求 | 实现位置 | 测试覆盖 |
|---------|------|---------|---------|
| FR-USER-01/02 | 注册+登录 | `router_auth.py:113-175` | `test_auth.py` (8 tests) |
| FR-USER-03 | PBKDF2 密码 | `router_auth.py:32-36` | 验证通过 |
| FR-USER-04 | /auth/me | `router_auth.py:179-197` | `TestAuthMe` (3 tests) |
| FR-USER-05 | 平台角色 admin/host/player | 注册+admin 接口 | `test_register_*` |
| FR-USER-06 | Admin 鉴权 | `router_admin.py` `_require_admin` | `test_admin_auth.py` (7 tests) |
| FR-USER-07 | Admin 改角色 | `router_admin.py:130-151` | `test_promote_*` |
| FR-USER-08 | Host 开房 | `router_rooms.py:20-22` | `test_room_security.py` |
| FR-USER-09 | Owner 三类鉴权 | `_verify_owner_or_admin` | `test_host_room_lifecycle.py` |
| FR-USER-10/11 | Host/Player WS | `main.py:280-334` | `test_ws_auth.py` (6 tests) |
| FR-USER-12 | Player token 反查 | `router_player.py` 所有端点 | `test_player_intent.py` |
| FR-USER-14 | session 恢复 | `restore-session` | `test_account_cannot_restore_others` |
| FR-USER-15 | 角色复制授权 | `router_player.py:273-283` | `test_copy_character_wrong_account_rejected` |
| FR-USER-16 | 私人资源隔离 | clues/objectives/archive | archive + reconnect tests |
| FR-USER-17 | RAG 鉴权入口 | `rag_router.py:_require_auth_for_room` | `test_rag_security.py` (10 tests) |
| FR-USER-18 | DTO 脱敏 | 公开接口+Admin `_char_detail` | DTO tests |
| FR-USER-19 | 前端多身份 slot | `identity.ts` | 开发口径明确 |

### ⚡ 本次补充 (Phase 1+2)

| 新增 FR | 实现 | 文件 |
|---------|------|------|
| **FR-USER-20**: 生产 bootstrap 阻断 | `BOOTSTRAP_ADMIN_CODE` 环境变量检查 | `router_auth.py:21,24,128-132` |
| **FR-USER-21**: JWT_SECRET fail-fast | 启动时拒绝默认密钥（开发模式 `AIKEEPER_DEV_MODE=1` 跳过） | `main.py:60-65` |

### 📋 演进方向（文档标注，本轮不实现）

| # | 项目 | 优先级 | 备注 |
|---|------|--------|------|
| 1 | token 生命周期 (rotate/revoke/hash) | P1 | 新 Batch User-2.5 |
| 2 | WS 短期票据 (ws_ticket) | P1 | 新 Batch User-3.5 |
| 3 | RAG 结果 visibility 裁剪 | P1 | FR-USER-24 |
| 4 | DTO 分层 (Public/Self/Admin/RoomMember) | P1 | PRD §DTO分层方向 |
| 5 | 错误码统一口径 (401/403/404) | P1 | PRD §错误码口径 |
| 6 | account/token 版本与封禁 | P2 | token_version, disabled_at |
| 7 | 多设备会话管理 | P2 | account_sessions |
| 8 | 细粒度 RBAC | P2 | capability-based |

---

## 三、生产安全基线对照

文档列出的 6 项 P0 安全缺口，当前状态：

| 基线 | 状态 | 说明 |
|------|------|------|
| 首账号 admin bootstrap | ⚡ 已保护 | `BOOTSTRAP_ADMIN_CODE` 可阻断裸部署抢注 |
| JWT_SECRET fail-fast | ⚡ 已保护 | 默认值拒绝启动；开发环境 `AIKEEPER_DEV_MODE=1` |
| WS 短期票据 | 📋 未关闭 | query token 仍是当前事实 |
| owner/player token 生命周期 | 📋 未关闭 | 明文存储、无 rotate/revoke |
| account token 撤销与封禁 | 📋 未关闭 | 有 exp，无 token_version/disabled_at |
| RAG 结果裁剪 | 📋 未关闭 | 成员级鉴权但不裁剪 chunk 内容 |

---

## 四、测试基线

```powershell
pytest tests/ -q
```

| 指标 | 数值 |
|------|------|
| 测试通过 | 409 |
| auth 相关 | 22 (test_auth + test_admin_auth + test_ws_auth) |
| 错误 (xlsx tmpdir) | 12 |
| 前端构建 | ✅ |

---

## 五、建议后续行动

1. **P1** — Batch User-2.5: token hash 存储 + rotate/revoke 方案
2. **P1** — Batch User-3.5: ws_ticket 短期票据替代 query token
3. **P1** — RAG 结果裁剪接入 visibility/audience 过滤
4. **P2** — DTO 分层结构 + 错误码统一口径
