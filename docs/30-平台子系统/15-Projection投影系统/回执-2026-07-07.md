# Projection 投影系统 — 文档更新回执

**日期:** 2026-07-07
**审查范围:** `功能列表.md` / `PRD.md` / `DeepSeek计划.md` / `验收文档.md`

---

## 一、本轮覆盖

Projection 模块 P0 主链路已通过前序 batch 闭环。本轮补强 `s2c_state_patch` 版本字段。

---

## 二、修改文件

| 文件 | 改动 |
|------|------|
| `src/server/engine/state_service.py` | `_write_state_patch_event`: payload 增加 `schemaVersion`/`baseStateVersion`/`stateVersion` |

### 前序 Batch 已闭环

| 能力 | Batch | 实现 |
|------|-------|------|
| 统一可见性 helper `_can_player_see_event` | TL-2 | `event_log.py` |
| `PLAYER_VISIBLE_SYSTEM_EVENTS` 白名单 | TL-2 | `event_log.py` |
| `get_events_for_player()` 安全过滤 | TL-2 | `event_log.py` |
| `get_public_events()` 白名单化 | JN-1 | `event_log.py` |
| 所有 `s2c_*` 入 registry | TL-3/JN-2 | `events_registry.py` |
| `EngineEventType` Literal 补全 | TL-3 | `models.py` |
| public export 用 `public_version` | Clue-3 | `export.py` |
| `s2c_state_patch` 带 `actionId` | ST-2 | `state_service.py` |
| `s2c_action_completed` 带 `turnId/roomId/characterId` | TX-4 | `resolution_pipeline.py` |
| Host reveal 安全化 | Map-5 | `router_host.py` |

---

## 三、system_safe 白名单

当前 `PLAYER_VISIBLE_SYSTEM_EVENTS`:
- `s2c_turn_resolved`
- `s2c_checkpoint_created`

其他 system 事件默认对 Player 不可见。`s2c_checkpoint_restored` 保持 `audit-only` (audience=host)。

---

## 四、测试基线

| 指标 | 数值 |
|------|------|
| 全量后端 | **409 passed** |
| 前端构建 | ✅ passed (901ms) |

---

## 五、验收自检

### 一票否决项

| 项 | 状态 |
|----|------|
| 统一 helper 未复用 | ✅ TL-2 |
| reconnect snapshot 返回整房间事件 | ✅ TL-2 |
| public export 用 `audience != 'player'` | ✅ Clue-3/JN-1 |
| system 事件默认对玩家可见 | ✅ 白名单 |
| `s2c_state_patch` 缺版本字段 | ✅ 本轮补全 |
| Projection 在 State 前投递 | ⚠️ 顺序未逆转（Pipeline） |
| Host frame 写回 events | ✅ 独立 HostStore |
| public export 含 token/raw/truth | ✅ 已脱敏 |

### 本轮工程验收清单 — ✅ 通过

---

## 六、结论

### 本轮工程验收：✅ 通过

统一可见性 helper、system 白名单、事件注册、导出脱敏、版本字段已全部闭环。

### 生产安全完成：⚠️ 未达到

- per-device ACK 未落地
- Pipeline 投影顺序未逆转
- direct write path 未完全收口
