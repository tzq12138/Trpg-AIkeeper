# Host Client 公共舞台端 — 文档更新回执

**日期:** 2026-07-08
**审查范围:** `功能列表.md` / `PRD.md` / `DeepSeek计划.md`

---

## 一、修改文件

无新增后端修改。Host Client 安全边界已通过前序 batch 闭环。

### 前序 Batch 已闭环（Host 相关）

| 能力 | Batch | 实现 |
|------|-------|------|
| `force_start` 硬化（reason + confirm + 审计事件） | 01-Room | `router_rooms.py` |
| Host REST 鉴权（X-Owner-Token + account token） | 01-Room / 02-User | `router_rooms.py` / `router_host.py` |
| Host WS 鉴权（owner_token + admin account） | 已有 | `host/ws_manager.py` |
| `force_move` 校验归属 + reason + audit | 09-Map | `router_host.py` |
| `reveal/hide` 节点归属校验 + mapVersion | 09-Map | `router_host.py` |
| `retry-turn` / skip 带 reason | 10-Timeline | `turn_manager.py` |
| checkpoint restore 强制 confirm+reason + audit | 11-Journal | `router_archive.py` |
| public/full export 分层脱敏 | 11-Journal / 08-Clue | `export.py` |
| `owner_token` 仅在服务端生成（前端不再自造） | 已有 | 后端返回 |
| 临时 NPC 创建 display_name 持久化 | 06-NPC | `encounter_persistence.py` / `EncounterPanel.tsx` |

---

## 二、验收清单

| 功能 | 状态 |
|------|------|
| Host 登录创建房间 | ✅ |
| Host Lobby 快照/ready/成员管理 | ✅ |
| force_start reason+confirm | ✅ 01-Room |
| Host REST/WS 鉴权 | ✅ 已有 |
| HUD 初始化 | ✅ |
| 公共叙事投影 | ✅ |
| 私密事件隔离 | ✅ Projection |
| 地图全图监管 + reveal/hide + force move | ✅ 09-Map |
| 遭遇 confirm/next-round/resolve | ✅ |
| checkpoint/restore/export | ✅ 11-Journal |
| 前端构建 | ✅ (834ms) |
| 中文可读 | ✅ |

---

## 三、已知遗留

| 项 | 说明 |
|----|------|
| `owner_token` localStorage | 开发态遗留，生产需切 secure session |
| WS query token | 长期 query `ownerToken`，未做短期 ticket |
| Database tab | 占位状态，需隐藏或标"开发中" |
| Host WS catch-up (`lastSequence`) | 前端传了但后端未消费 |
| HostStore 持久化补全 | 当前仅覆盖部分舞台状态 |

---

## 四、测试基线

| 指标 | 数值 |
|------|------|
| Host 后端测试 | **65 passed** |
| 前端构建 | ✅ passed (834ms) |

---

## 五、结论

### 本轮工程验收：✅ 通过

Host Client 后端安全边界全部由前序 01/02/06/09/10/11 batch 闭环。前端构建通过，无新增代码改动。
