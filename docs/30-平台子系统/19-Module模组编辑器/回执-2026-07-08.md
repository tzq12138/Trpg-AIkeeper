# Module 模组编辑器 — 文档更新回执

**日期:** 2026-07-08
**审查范围:** `功能列表.md` / `PRD.md` / `DeepSeek计划.md` / `验收文档.md`

---

## 一、修改文件

无新增代码。Module P0 安全边界已通过前序 batch 闭环。

### 前序 Batch 已闭环

| 能力 | Batch | 实现 |
|------|-------|------|
| PDF 导入 admin-only + SHA256 去重 | 07-WorldBook | `router_scenarios.py` |
| 导入生成 knowledge_graph + quality_report | 07-WorldBook | `router_scenarios.py` |
| `importStatus` 四态（structured/requires_ocr/failed/already_imported） | 07-WorldBook | `router_scenarios.py` |
| blocked 模组不进 Host available | 07-WorldBook | `router_scenarios.py` |
| create-room quality gate（blocked→403, highRisk→confirm） | 07-WorldBook | `router_scenarios.py` |
| MapGenerator 优先 knowledge_graph.scenes | 09-Map | `router_admin.py` |
| 上传校验（MIME/大小/扩展名白名单/文件头） | 18-Asset | `router_admin.py` |
| templates 存在（join-info 端点） | 已有 | `router_player.py` |
| templates 不暴露 truth/hidden（表结构干净） | 已有 | `db_adapter.py` |
| 触发器 schema 校验 | 04-Rule | `rule_executor.py` |

---

## 二、验收自检（一票否决项）

| 项 | 状态 |
|----|------|
| `structured` 被当作"已可开房" | ✅ publishStatus 独立于 importStatus；blocked 被 available 排除 |
| blocked 模组下发给 Host | ✅ 07-WorldBook 已排除 |
| create-room 绕过 blocked/highRisk | ✅ 07-WorldBook quality gate |
| templates 返回 truth/hidden/adminNotes | ✅ 表结构无此类字段 |
| Host/Player 收到 scenarios 原始记录 | ✅ available 返回瘦 DTO |
| 地图生成只依赖旧字段 | ✅ 09-Map 优先 knowledge_graph.scenes |
| 坏触发器拖垮回合 | ✅ RuleExecutor 处理 |
| Room 运行态反写模板 | ✅ 无此类写入路径 |

---

## 三、已知遗留

| 项 | 说明 |
|----|------|
| `publishStatus` 独立字段 | 未新增，当前派生自 quality_level |
| `module_versions` | 未实现 — active room 绑定 mutable scenario，版本变更会影响运行中房间的 AI/RAG |
| `/api/scenarios/{id}/templates` 独立端点 | 当前仅通过 join-info 暴露 |
| `AdminModuleDetailDTO` | 未建立聚合 DTO |
| 触发器 `validationStatus` | 未实现 |
| debug/raw export 与 public share 分层 | 未区分 |

---

## 四、测试基线

| 指标 | 数值 |
|------|------|
| Module 相关后端测试 | **93 passed** (8 xlsx errors, unchanged) |
| 前端构建 | ✅ passed |

---

## 五、结论

### 本轮工程验收：✅ 通过

导入状态口径、发布准入（blocked/highRisk）、模板安全、地图生成源对齐全部由 07/09/18 batch 闭环。
