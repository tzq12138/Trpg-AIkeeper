# Rule 规则与骰子系统 — 文档更新回执

**日期:** 2026-07-06
**审查范围:** `功能列表.md` / `PRD.md` / `DeepSeek计划.md` 最新版本
**实现基线:** 探索发现 3 个 P0 成功等级不匹配，已全部修复

---

## 一、P0 修复

### Fix 1: CocSkillCheckHandler 成功等级统一
| 修复前 | 修复后 |
|--------|--------|
| `critical_success`, `success`, `failure`, `fumble` | `critical`, `extreme`, `hard`, `regular`, `failure`, `fumble` |
| `random.randint(1,100)` | 复用 `roll_skill_check()`（CoC 十位骰引擎） |
| bonus_dice 未实现 | bonus_dice 接入主链路 |
| 不区分 extreme/hard/regular | 全部六种等级正确返回 |

**文件:** `src/server/rules/coc_handlers.py:15-55`

### Fix 2: Pipeline 写入 success_level + successLevel
`_action_completed_payload` 之前只写 `"level"` 键，前端读取 `success_level ?? successLevel ?? 'regular'` → 永远走 `regular` fallback。

现在同时写入 `"level"`, `"success_level"`, `"successLevel"` 三个键。

**文件:** `src/server/engine/resolution_pipeline.py:317-332`

### Fix 3: _determine_success 增加低技能大失败规则
CoC 7e: skill < 50 时 roll >= 96 为 fumble（之前缺失）。

**文件:** `src/server/engine/skill_check.py:53-55`

---

## 二、已覆盖的文档要求

| 批次 | 内容 | 状态 |
|------|------|------|
| Rule-0 | 规则口径盘点 | ✅ 探索完成 |
| Rule-1 | 统一成功等级枚举 | ⚡ P0 修复 |
| Rule-2 | bonus_dice 接入主链路 | ⚡ P0 修复 |
| — | Pipeline 正确写 success_level | ⚡ P0 修复 |

---

## 三、演进方向

| # | 项目 | 优先级 |
|---|------|--------|
| 1 | RollResult 统一结构 (rollId/visibility/source) | P1 |
| 2 | `/api/player/skill-check` 不写 action/event 风险 | P1 |
| 3 | skillValue 权威来源锁死 | P1 |
| 4 | parse_dice 扩展 XdY+N | P1 |
| 5 | 暗骰/私密骰 visibility 系统定义 | P1 |
| 6 | 触发器 schema 校验 | P1 |
| 7 | test_rule_executor 覆盖补全 (仅 2 tests) | P1 |
| 8 | Handler 用 random 无 RollRecord | P2 |

---

## 四、测试基线

| 指标 | 数值 |
|------|------|
| Rule 测试 | 54 passed |
| 全量测试 | 409 passed |
| 错误 (xlsx) | 12 |
