# 交付记录 B4 —— 服务端准备证据（探测）与初始策略冻结（D19 / AIO-SZ-001~008）

> 模板按 04 第 5 节；原始日志 `.runtime/p0-handoff/b4*.log`。

| 字段 | 真实值 |
|---|---|
| 任务 ID / 执行者 / 日期 | B4 / 交接执行（tzq12138 工作区 codex）/ 2026-09-05 |
| 起点 / 终点 | 起点 B3 提交后；终点见本批提交（`feat: persist session zero probe evidence and freeze absent policy (B4)`） |
| 解决的问题 | 触发：五个确认按钮+ready 不代表私密/公共投影真的送达本人、缺勤策略可随时改、无服务端探测证据。原行为→新行为：新表 `session_zero_probes`/`absent_policy_snapshots`；connection 步骤确认时服务端发出 private/party/device_recovery 探测（含 events 投影记录与水位）；探测须由角色当前控制器设备确认（device_recovery 须真实 reconnect 水位）；开始门禁与 GET complete 共用 Engine 计算（探测无效即阻断/重算）；start 事务冻结 v1 缺勤快照，之后 PATCH 追加版本化快照 |
| 修改文件 | 服务端：`src/server/db_adapter.py`（两张新表幂等 DDL）；新建 `src/server/engine/session_zero_probes.py`（issue/confirm/gate/validity）；`router_campaign_v2.py`（GET complete 纳入探测、connection 后发探测、GET /probes、POST /probes/confirm）；`router_player_settings.py`（PATCH 追加快照版本）；`router_rooms.py`（门禁加 probe_* 缺项、start 事务冻结 v1 快照）；测试 `tests/server/test_session_zero_runtime_probes.py`（新建 7 例）+ `test_glass_rain_golden_flow.py`（共享 helper 补真实 claim+探测确认，device id 用 legacy 回退一致值防后续草稿确认冲突）；前端：`PlayerLobby.tsx`（五步完成后自动 claim(不接管)+确认探测+真实 reconnect 水位，失败给出接管/重试原因）、`player-api.ts`/`types.ts`（probes API 与 DTO、complete 语义字段） |
| 接口/DDL 变化 | 新表 `session_zero_probes`、`absent_policy_snapshots`（幂等 CREATE+INDEX，空库/已有库/重复初始化均可）；GET session-zero 响应增 `probe_complete`/`missing_probes`；GET/POST `/api/player/session-zero/probes[/confirm]`；409 码 probe_not_found/probe_wrong_device/probe_watermark_insufficient/probe_expired/probe_type_mismatch |
| 红例证据 | 探测门禁生效前 golden 共享 helper 开始被拦（409 AI_ONLY_SESSION_ZERO_INCOMPLETE probe_*）；four-player 流程与 helper device 撞车（controller_held 409）已通过统一 legacy device id 解决。日志 `.runtime/p0-handoff/b4*-*.log` |
| 定向测试 | `test_session_zero_runtime_probes.py` 7 例 + golden flow + background runtime + reconnect + action_reviews_v2 → **52 passed**；四玩家 16 例通过（除下述既有红例） |
| 全量测试/构建 | 前端 `npm test`（lobby/host-lobby 相关）与 `npm run build` ✓（全量 221 在 B2 已验，B4 前端增量后 8/8 定向绿）；后端全量归 Wave 出口 |
| 浏览器与 Provider | 不适用（服务端证据链自动化验证；真实浏览器路径 B5） |
| 故障/恢复 | 不适用 |
| 遗留问题 | **既有红例（B3 提交上已复现，非本任务引入，按归属后续处理）**：① `test_campaign_v2.py::test_shared_hypothesis_...stage` stage-projection 403（admin Bearer 不再被 `_verify_stage_client` 接受）；② `test_glass_rain_golden_flow.py::test_glass_rain_v2_ai_only_success_mixed_and_safe_abort_flows` 结局后预备反应被处理（reject_post_ending_prepared_write）；③ `test_glass_rain_four_player_flow.py::...complete_authoritative_runtime` ending metadata 缺 `verified_ending`（受②影响的结局路径）。均归引擎结局/舞台鉴权归属，R/V 阶段处理前保持红并记录 |
| 下一任务合同 | B5：`test_ai_only_opening_flow.py`（新房间→加入→claim→五步→探测确认→start→提交→analyze→confirm 幂等→回执关联）；真实浏览器双身份路径 |
| 回退方式 | 回退本批提交（含 DDL 增量，回退不破坏旧表；新表可留空） |
