# 交付记录 B5 —— 双人无 Stage 首次行动回执（API 全链）

> 模板按 04 第 5 节。

| 字段 | 真实值 |
|---|---|
| 任务 ID / 执行者 / 日期 | B5 / 交接执行（tzq12138 工作区 codex）/ 2026-09-05 |
| 起点 / 终点 | 起点 B4 提交后；终点见本批提交（`test: verify two-player no-stage opening flow receipts (B5)`） |
| 解决的问题 | 触发：第一笔交付（新玩家加入→确认→开始→首行动回执）无纵向 API 回归。原行为→新行为：新 `test_ai_only_opening_flow.py` 从真实开局门禁（含 B4 探测）出发，两玩家无 Stage 完成 dialogue 行动（无骰）与 skill_check 行动（原骰回执），重复 confirm 幂等 |
| 修改文件 | `tests/server/test_ai_only_opening_flow.py`（新建，复用 golden/four-player 已证明的 analyze/confirm/流水线形状） |
| 接口/DDL 变化 | 无 |
| 红例证据 | 首轮红：房间快照 status 断言错误（lobby vs active，已改查库）；receipt 恒带 `verification_receipt` 键（无骰为 null，断言改为 null/非 null）；skill 行动需 `_PersuadeCompiler`（golden compiler 会把 skill_check 编成 dialogue）；host 裁决断言过宽（实际为 `s2c_director_plan_validated` 审计投影，改为裁决事件集检查）。日志 `.runtime/p0-handoff/b5*.log` |
| 定向测试 | `test_ai_only_opening_flow.py` + `test_action_drafts_v2.py` + `test_room_risk_contract.py` + `test_reconnect.py` → **92 passed, exit 0** |
| 全量测试/构建 | 前端无改动（B5 前端 = Lobby/控制台已在 B2/B3/B4 接线，构建已验）；后端全量归 Wave 出口 |
| 浏览器与 Provider | **真实浏览器双身份路径未执行**：按执行顺序与 03 手册，浏览器证据与 R7/V4（BR-2-01 等）共享真实环境准备（Playwright + 独立验证库 + 真实 Provider），归 Wave 2/3 统一浏览器阶段执行；本阶段记录为"浏览器/AI 待验"，不写完成。API 侧全部经真实 PostgreSQL + 确定性 Gateway 证据 |
| 故障/恢复 | 不适用（B5 无故障注入；恢复属 R 系列） |
| 遗留问题 | 浏览器双身份全 UI 路径（加入→确认→探测→开局→首行动→Owner 关页后续行动）待统一浏览器阶段补证据；首笔交付验收以 API 全链 + 浏览器补证后合并判定 |
| 下一任务合同 | Wave 2 R1（真实故障暂停→paused_system 对齐、协作批次暂停入口统一、事件真实分发） |
| 回退方式 | 回退本批提交（纯测试文件） |
