# 交付记录 B2 —— Session Zero 面板接入 PlayerLobby

> 模板按 04 第 5 节。

| 字段 | 真实值 |
|---|---|
| 任务 ID / 执行者 / 日期 | B2 / 交接执行（tzq12138 工作区 codex）/ 2026-09-05 |
| 起点 / 终点 | 起点 B1 提交后；终点见本批提交（`feat: mount Session Zero panel in PlayerLobby (B2)`） |
| 解决的问题 | 触发：新玩家加入 Lobby 无任何 Session Zero 入口（面板只在运行页 CampaignHomePanel），服务端要求先完成确认才能开局 → 死循环。原行为→新行为：Lobby 左侧"开局准备台"内嵌 SessionZeroPanel，加入者可原地完成 5 步确认；确认后重新拉取服务端状态；乱序/旧 hash 失败保留可理解文案并刷新面板供重试 |
| 修改文件 | `src/client/src/pages/PlayerLobby.tsx`（导入并挂载 SessionZeroPanel，状态 sessionZero/sessionZeroError，refreshSessionZero 与 confirmSessionZeroStep 处理器，加载失败重试按钮）；`src/client/tests/player-lobby-session-zero.test.tsx`（新建，jsdom 挂载式 4 用例）；`src/client/package.json` + `package-lock.json`（新增 devDependency jsdom，文档 01 B2 要求补真实 DOM 环境） |
| 接口/DDL 变化 | 无服务端变化；复用既有 `GET /api/player/session-zero`、`POST /api/player/session-zero/{step}` 与 `SessionZeroPanel` 组件签名（CampaignHomePanel.tsx:398 导出，未改） |
| 红例证据 | 首轮 jsdom 挂载缺 `scrollIntoView`（jsdom 未实现 → 测试补 stub）；stale-confirm 文案被随后成功 re-sync 清除（组件逻辑缺陷：确认失败后需保留错误文案；已修）。日志见 `.runtime/p0-handoff/`（b2 前端 vitest 输出） |
| 定向测试 | `npm test -- tests/player-lobby-session-zero.test.tsx tests/campaign-home-panel.test.tsx tests/player-api.test.ts` → **40 passed, exit 0**（4 新增 + 36 既有保留） |
| 全量测试/构建 | `npm test` → **56 files / 221 passed, exit 0**（基线 217 + 新增 4）；`npm run build` → tsc + vite 通过（✓ built） |
| 浏览器与 Provider | 不适用（挂载级 jsdom 证据；真实浏览器路径属 B5） |
| 故障/恢复 | 不适用 |
| 遗留问题 | 面板内样式沿用 CampaignHomePanel 的 bh-campaign-home__section 类（无专属 lobby 样式调整，视觉可接受）；乱序/旧 hash 的服务端 409 语义已在既有 server 测试覆盖 |
| 下一任务合同 | B3：GET `/api/rooms/{id}` 增量返回 `session_mode`；HostLobby 结构化渲染 409 missing 数组；成功 Start 后不再强制跳 Stage |
| 回退方式 | 回退本批提交；jsdom 依赖可保留（不影响构建产物） |
