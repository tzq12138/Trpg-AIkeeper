# 交付记录 B3 —— Host 缺项显示与 Stage 可选

> 模板按 04 第 5 节。

| 字段 | 真实值 |
|---|---|
| 任务 ID / 执行者 / 日期 | B3 / 交接执行（tzq12138 工作区 codex）/ 2026-09-05 |
| 起点 / 终点 | 起点 B2 提交后；终点见本批提交（`feat: show host readiness gaps and make Stage optional (B3)`） |
| 解决的问题 | 触发：Host 开局失败时 `String(data.detail)` 把结构化 409 渲染成 `[object Object]`；成功后强制跳 Stage（HostLobby.tsx:308 原逻辑）；前端无法得知服务端 session_mode。原行为→新行为：GET `/api/rooms/{id}` 返回服务端认定 `session_mode`；409 `AI_ONLY_SESSION_ZERO_INCOMPLETE` 结构化渲染逐玩家缺项（安全边界/连接确认等中文标签）；成功 Start 后进入 `/host/{roomId}/console`（原导演控制台入口），Stage 仅作可选链接，Stage token 获取失败不影响已成功的开局 |
| 修改文件 | `src/server/router_rooms.py`（GET room 响应 +`session_mode`，NULL 归一 `""`）；`tests/server/test_rooms.py`（新增 `test_get_room_reports_server_derived_session_mode`）；`src/client/src/pages/HostLobby.tsx`（RoomData+session_mode；抽出可测纯组件 `SessionZeroMissingBlock`；错误分流：结构化 detail→missing 数组、reason 字符串、旧字符串兼容；ai_only 隐藏强制开始框与 HostAutonomyPolicyControl；成功导航到 console）；`src/client/tests/host-lobby-session-zero.test.tsx`（新建 jsdom 挂载 4 用例） |
| 接口/DDL 变化 | 响应增量字段 `session_mode`（无破坏性）；无 DDL。不新增第二套 readiness API（沿用 409 detail 合同） |
| 红例证据 | 首轮服务端测试：`result["session_mode"]` 为 None（列 NULL 未归一 → 已修）。日志 `.runtime/p0-handoff/b3-server-*.log` |
| 定向测试 | 服务端：`test_rooms.py` → 29 passed exit 0；`test_ai_only_force_start_cannot_bypass_session_zero_or_readiness`（glass_rain golden）→ passed。前端：host-lobby-session-zero + player-lobby-session-zero + host-launch-checklist → 10 passed exit 0 |
| 全量测试/构建 | 前端 `npm run build` → tsc+vite ✓；全量前端 221 在 B2 已验（本任务新增 4 例）；后端全量归 Wave 出口 |
| 浏览器与 Provider | 不适用（挂载级证据；真实"全程无 Stage"浏览器路径属 B5） |
| 故障/恢复 | 不适用 |
| 遗留问题 | force_start 门禁未变（服务端仍硬拒 ai_only）；HostAutonomyPolicyControl 仅隐藏不删除（非 ai_only 房间保留原逻辑） |
| 下一任务合同 | B4：探测证据表与策略冻结（服务端为主，前端 Lobby 接收/展示自己探测） |
| 回退方式 | 回退本批提交；无 DDL 迁移 |
