# 交付记录 B1 —— 统一后台 Pipeline 装配与自动幂等结算

> 模板按 04 第 5 节；原始日志在 `.runtime/p0-handoff/b1-*.log`。

| 字段 | 真实值 |
|---|---|
| 任务 ID / 执行者 / 日期 | B1 / 交接执行（tzq12138 工作区 codex） / 2026-09-05 |
| 起点 / 终点 | 起点 `9f6da0b`+B0 记录提交（B0 交付后）；终点见本批提交（`fix: unify background pipeline wiring and secure turn projection (B1)`） |
| 解决的问题 | 触发：后台装配不一致（单行动缺 spoiler_guard/cache，协作批次连 state_service/gateway 都缺，批次撞 `state_service_unavailable`）；`s2c_turn_resolved` 广播把原始 `declared_intent`/`result.mutations` 发到 party；两个收集阻断符号缺失。原行为→新行为：见"修改文件"列；收集错误清零（B1 定向 6 文件全绿） |
| 修改文件 | `src/server/player/router_player.py`（新增 `_create_background_pipeline(app, conn)` 统一工厂：dispatcher 带 cache+spoiler_guard、StateService/Pipeline 同实例同 conn；单行动 `_resolve_action_background` 与协作 `_resolve_collaboration_batch_background` 均改走工厂；新增 `build_turn_resolved_projection`，替换 1423 一带裸广播为安全投影 {turnId,actionCount,summary}）；`tests/server/test_action_resolution_scheduler.py`（迁移旧 batcher 断言，理由见 04 §6）；`tests/server/test_background_resolution_runtime.py`（保留原工厂契约测试 + 新增两条真实异步回归） |
| 接口/DDL 变化 | 无 DDL。行为：`s2c_turn_resolved` 非战斗载荷由 {turn_id,narrative,actions} 改为 {turnId,actionCount,summary}——客户端只读 `combatRoundSummaryText`（战斗分支 payload 未变），已核兼容，无需前端改动。工厂不含 host_connection_checker：pipeline `_is_host_connected` 对 None 返回 True（视为 Host 在线），符合 01 装配示意 |
| 红例证据 | `.runtime/p0-handoff/b1-red-*.log`：`AttributeError: ... has no attribute '_create_background_pipeline'`（test_background_resolution_runtime.py:34）；`RuntimeError: no running event loop`（调度测试同步调用 create_task）；收集阻断 `ImportError: cannot import name 'build_turn_resolved_projection'`。全部退出码 1/2 |
| 定向测试 | `pytest test_background_resolution_runtime.py test_action_resolution_scheduler.py test_turn_settlement_projection.py test_room_action_batcher.py test_action_lifecycle_v2.py test_host_autonomy.py` → **39 passed, exit 0**（`.runtime/p0-handoff/b1-green3-*.log`）。含两条真实异步回归：并发双 worker 单 action → 1 trace/1 resolving/state_version+1；协作批次状态变更无 `state_service_unavailable`、party 无原始结果内部字段 |
| 全量测试/构建 | 未跑全量（B1 范围外，B5/Wave 出口统一执行）；收集错误已随实现清零 |
| 浏览器与 Provider | 不适用 |
| 故障/恢复 | 不适用（并发幂等属 B1 双 worker 回归覆盖） |
| 遗留问题 | RoomActionBatcher 判定不接入生产（理由见 04 §6 处置 1，模块与自测保留）；泄漏断言按结构化字段收紧（§6 处置 2，公开行动目标播报属合法公开叙事）；batcher/日志型"module imported but unused"归属实验能力单列 |
| 下一任务合同 | B2 消费 GET/POST session-zero 既有接口与 `SessionZeroPanel`（CampaignHomePanel.tsx:398 导出），在 PlayerLobby 挂载 |
| 回退方式 | 回退本批提交即可；无 DDL 迁移约束 |
