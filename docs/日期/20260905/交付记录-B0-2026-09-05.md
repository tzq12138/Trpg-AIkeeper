# 交付记录 B0 —— 接手基线、依赖与失败归属

> 交付模板按 [04_任务看板与交付模板.md](./04_任务看板与交付模板.md) 第 5 节填写。原始日志/证据在 `.runtime/p0-handoff/`（不入 Git）。

| 字段 | 填写要求（真实值） |
|---|---|
| 任务 ID / 执行者 / 日期 | B0 / 交接执行（tzq12138 工作区，codex 执行） / 2026-09-05 |
| 起点 / 终点 | 起点 HEAD `4b22cbe`（工作树仅有 3 份交接文档编写期补记 +6/−1 行）；结束 commit `9f6da0b`（依赖/文档）+ 本记录所在提交（`docs: record B0 delivery baseline`，随看板日志可查） |
| 解决的问题 | 环境不可复现（PATH Python 为 Hermes venv，缺 `mcp.server.fastmcp`）；收集错误 2 个（MCP 环境 + `build_turn_resolved_projection` 不存在）。触发→原行为→新行为：00 文档环境预检 → Hermes 环境缺 fastmcp/主依赖缺口 → 建专属 venv `.runtime/p0-venv`（Python 3.11.15）并补直接依赖后，收集从 2 错误收敛为 **1 错误（纯主链缺口）** |
| 修改文件 | `pyproject.toml`（+defusedxml>=0.7.1、+numpy>=1.26.0，均为顶层直接 import 缺口）；`uv.lock`（uv lock 重新解析，43→44 包）；`kp_mcp_server/requirements.txt`（`mcp>=1.0.0,<2`——代码走 v1 `mcp.server.fastmcp` API，mcp 2.x 已移除该模块；+`openai>=1.0.0`——kp_brain.py 顶层 import）；`docs/日期/20260905/00/02/03`（编写期补记，内容核对无业务影响）；`04_任务看板与交付模板.md`（看板状态） |
| 接口/DDL 变化 | 无业务接口/DDL 变化。依赖声明修复理由如上（00 允许"确有必要的依赖修复"） |
| 红例证据 | 收集前两轮失败：`ModuleNotFoundError: No module named 'mcp.server.fastmcp'`（mcp 2.x 改名）；`No module named 'numpy'`（embedding.py 顶层）；`No module named 'openai'`（kp_brain.py）。日志：`.runtime/p0-handoff/b0-collect-*.log` |
| 定向测试 | 不适用（B0 无业务定向测试） |
| 全量测试/构建 | **收集**：`pytest tests/server --collect-only` → **1731 collected, 1 error, exit 2**（唯一错误 `build_turn_resolved_projection` ImportError，属主链缺口 → B1）。完整回归未执行（B0 范围）；原评审"2 个收集错误"中的 MCP 环境错误已随专属环境消失，以本次实际重跑为准 |
| 浏览器与 Provider | 不适用：B0 未跑浏览器/Provider |
| 故障/恢复 | 不适用 |
| 遗留问题 | 主链缺口归属：`build_turn_resolved_projection` 缺失（B1 安全投影替换裸广播）；`_create_background_pipeline` 缺失（B1 统一装配）；actions/intake 405、resolution-console 404（实验接口，不扩 M0，按 00/01 单列归属）；CLAUDE.md 对 run_golden_module_suite.py 的 `--spec` 文档与 CLI 不符（非本任务范围，V3 前处理） |
| 下一任务合同 | B1：`router_player.py` 新增 `_create_background_pipeline(app, conn)`（dispatcher 带 cache+spoiler_guard、StateService/Pipeline 同实例同 conn）；`build_turn_resolved_projection` 只发 `{turnId, actionCount, summary}` |
| 回退方式 | `git revert 9f6da0b`（依赖声明）；venv 属忽略目录 `.runtime/`，可删除重建 |

## 环境事实（复现要点）

- 专属解释器：`.runtime/p0-venv/Scripts/python.exe`（Python 3.11.15）；测试终端环境：`TEST_DATABASE_URL`/`DATABASE_URL`=`postgresql://aikeeper:aikeeper123@localhost:5432/aikeeper_p0_executor_test`、`AIKEEPER_DEV_MODE=1`、`AI_CONFIG_MASTER_KEY=aikeeper-p0-test-only-key`、`PYTHONUTF8=1`。
- PG/Redis 由 docker compose 提供（healthy）；`DEEPSEEK_API_KEY` 已设。
- 归属表与基线捕获：`.runtime/p0-handoff/b0-*.{log,txt}`。
