# 交付记录 R1 —— 状态统一与真实阶段暂停

> 模板按 04 第 5 节。

| 字段 | 真实值 |
|---|---|
| 任务 ID / 执行者 / 日期 | R1 / 交接执行（tzq12138 工作区 codex）/ 2026-09-05 |
| 起点 / 终点 | 起点 B5 提交后；终点见本批提交（`fix: align system pause with runtime states (R1)`） |
| 解决的问题 | 触发：`_pause_room_for_integrity` 把可恢复动作终结为 rejected、只写旧 rooms.status，system_recovery 门禁（runtime_status=paused_system）不可达；协作批次 infra 失败走独立旧式暂停。原行为→新行为：单行动与协作批次同一系统暂停语义（同事务写 paused_system+legacy status+integrity 原因），动作保留 resolving 与已提交产物，删除伪造 rejected 完成事件；`s2c_room_paused` 入 EngineEventType 真实 WS 分发；门禁拦截 paused_system/recovering；恢复 API 从真实故障入口可达（红例断言 proposal 创建成功） |
| 修改文件 | `src/server/engine/room_pause.py`（pause_blocks_new_actions 扩展、新增 `pause_room_for_system_integrity`/`sync_legacy_room_status`、resume 后 legacy 对齐）；`src/server/engine/resolution_pipeline.py`（`_pause_room_for_integrity` 重写：保留动作、新事件载荷）；`src/server/player/router_player.py`（协作批次 ai_only infra 失败改走统一暂停并保留 batched 动作）；`src/server/engine/system_recovery.py`（完成后 legacy status 对齐）；`src/server/models.py`（EngineEventType 增 s2c_room_pause_requested/room_paused/room_resumed）；`src/server/ai/gateway.py`（ProviderFailure.retryable + 瞬时错误标记 + as_dict）；测试 `tests/server/test_system_failure_recovery.py`（新建真实故障→恢复红例）、`test_ai_gateway.py`（retryable 断言） |
| 接口/DDL 变化 | 无 DDL。事件 `s2c_room_paused` 载荷改为 {reasonCode, mode:system_paused, actionId} 并对 party 真实分发；`s2c_ai_recovery_required` 对所属玩家分发。ProviderFailure 增 `retryable` 字段（as_dict 同步） |
| 红例证据 | 复现路径见 `.runtime/p0-handoff/r1*.log`：初版注入点 `_apply_verified_narration_fallback` 异步化未生效（fallback 为同步方法）→ 改用真实 TimeoutError 网关 + `_can_use_verified_narration_fallback=False` 使 `narrator_timeout` 错误返回 → 触发 pause 分支；proposal 端点 201 Created（响应嵌套 proposal 键）修正断言 |
| 定向测试 | `test_system_failure_recovery.py` + `test_ai_gateway.py` + `test_runtime_integrity.py` → **52 passed, exit 0**；glass_rain_golden_flow 回归：除既有红例（v2 ending prepared-reaction，B3 提交上已复现）外全绿 |
| 全量测试/构建 | 未跑全量（R 系列范围内，Wave 出口统一执行） |
| 浏览器与 Provider | 不适用（故障适配器仅测试启用；真实进程 kill/restart 属 R3/R7） |
| 故障/恢复 | 红例注入点：narrator TimeoutError（Provider 故障）+ fallback 资格关闭；断言 paused_system/legacy paused/动作 resolving/无伪造完成事件/room_paused party 分发/前置骰回执保留/恢复 proposal 可达/新行动 409 |
| 遗留问题 | golden v2 ending 既有红例仍在（结局后预备反应，非 R1 范围，归结局/预备反应归属）；R2 journal 尚未接入（resume 实际续跑依赖 R2/R3） |
| 下一任务合同 | R2：`resolution_journal.py`（claim/load/save_stage）+ `action_resolution_runs`/`resolution_effects` 表，供 R3 `resume_action` 消费 |
| 回退方式 | 回退本批提交；无 DDL 约束 |
