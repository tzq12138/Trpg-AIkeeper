# 交付记录 V2 —— 修复缺指标发布门禁

| 字段 | 真实值 |
|---|---|
| 任务 ID / 执行者 / 日期 | V2 / 交接执行（tzq12138 工作区 codex）/ 2026-09-05 |
| 起点 / 终点 | 起点 R2 提交后；终点见本批提交（`fix: block release on missing benchmark evidence (V2)`） |
| 解决的问题 | 触发（F6 已复现）：30 份空观测（无行动/Trace/延迟/评分）仍 `release_ready=true`（阈值仅 value is not None 判违规）。原行为→新行为：`aggregate_benchmark(observations, *, browser_observations=())` 仿真与浏览器独立统计；缺证据独立阻断（`missing_metric_evidence`：必测比率分母 0/无延迟/无评分/无浏览器场；`missing_trace_evidence`：无进流水线行动或行动零完整 Trace）；非法值阻断（重复 session、NaN/Inf 延迟、负计数、Trace complete>总数、评分出界 → `invalid_observation_values`）；空浏览器评分 → `browser_evidence_invalid`；`_healthy_observations` 改为 30 仿真 + 2 独立浏览器观测（sample.sessions 仍 30） |
| 修改文件 | `src/server/scenario/ai_only_benchmark.py`、`tests/server/test_ai_only_benchmark.py`（6 旧用例适配 + 8 新反例，14 passed exit 0） |
| 接口/DDL 变化 | 聚合器签名增关键字参数 browser_observations（默认空元组，向后兼容单参调用）；新增阻断码见上 |
| 红例证据 | 反例测试先红（30 空观测 release_ready=true），日志 `.runtime/p0-handoff/v2*.log` |
| 定向测试 | `test_ai_only_benchmark.py` → 14 passed exit 0 |
| 遗留问题 | 版本束一致性校验属 V3 evidence manifest 层（聚合器无版本束字段，无法在纯观测层断言，按 03 文档归属 V3 validate_evidence_manifest）；真实场次未跑（V4） |
| 回退方式 | 回退本批提交 |
