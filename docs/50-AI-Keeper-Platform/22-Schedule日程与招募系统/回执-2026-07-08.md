# Schedule 日程与招募系统 — 文档更新回执

**日期:** 2026-07-08
**审查范围:** `功能列表.md` / `PRD.md` / `DeepSeek计划.md` / `验收文档.md`

---

## 一、当前状态

无新增代码。Schedule 的 P0 主链路（入房、审批、ready、force_start、lobby 快照）全部已由现有基础设施实现。

### 已闭环能力

| 能力 | 实现位置 |
|------|----------|
| `join-info` 返回 clean DTO（不含 token/truth/hidden） | `router_player.py:69-127` |
| `join_mode`: draft→direct, lobby→direct, active→host_approval | `router_player.py:90-96` |
| active 加入 → `pending_approval` | `router_player.py` join 逻辑 |
| `pending_approval` 不计入 ready/start | `turn_manager.py` (joined only) |
| approve/reject 端点 | `router_host.py` |
| approve/reject 审计 + lobby 广播 | `router_host.py` |
| `force_start` reason+confirm+audit | `router_rooms.py` (01-Room) |
| `ready_toggle` 只改自己 | `engine.py` |
| lobby snapshot 不含 token | `s2c_room_lobby_snapshot` |
| `recruit_posts`/`schedule_polls` 未实现 | ✅ 不误做 |

---

## 二、验收自检（一票否决项）

| 项 | 状态 |
|----|------|
| join-info 不含 owner_token/player_token/truth/hidden | ✅ |
| active 加入 → pending_approval | ✅ |
| pending 不计入 ready/start | ✅ TL-1 |
| pending 不能 submit intent | ✅ (status != 'joined') |
| force_start confirm+reason | ✅ 01-Room |
| approve/reject 审计 | ✅ |

---

## 三、已知遗留

| 项 | 说明 |
|----|------|
| reject 后 token 生命周期 | 未显式失效（P1） |
| Schedule-4/5（私域招募/日程投票） | 未进入本轮 |
| 通知系统 | 未实现 |

---

## 四、测试基线

| 指标 | 数值 |
|------|------|
| Schedule 相关测试 | **59 passed** (8 xlsx errors) |
| 前端构建 | ✅ |

---

## 五、结论

### 本轮工程验收：✅ 通过
