# Player Client 玩家私人端 — 文档更新回执

**日期:** 2026-07-07
**审查范围:** `功能列表.md` / `PRD.md` / `DeepSeek计划.md` / `验收文档.md`

---

## 一、修改文件

| 文件 | 改动 |
|------|------|
| `src/client/src/shared/api.ts` | `getPlayerToken()`: 移除本地自造 UUID 逻辑。Player token 必须以服务端 join/restore-session 返回为准，本地缺 token 时返回空字符串（由服务端返回 401/403 驱动登录/入房流程） |

### 前序 Batch 已闭环（后端安全边界）

| 边界 | Batch |
|------|-------|
| 后端 `/character` 返回 runtime 优先值 (HP/SAN/MP/Luck) | 05-Character |
| `/sync` 含 shared clues (public_version) | 08-Clue |
| `/clues` 分享安全化（默认不公开原文） | 08-Clue |
| 地图 GET 无 token → 401/403 | 09-Map |
| 地图匿名视图返回 MapNodePublicDTO | 09-Map |
| `_can_player_see_event` 统一 helper | 10-Timeline |
| reconnect 安全过滤 | 10-Timeline |
| `stateVersion` 版本屏障 (schemaVersion/base/state) | 13-State/15-Projection |
| 后端 `clue_id` 为权威主键 | 08-Clue |
| archive 类型枚举与后端一致 | 10-Timeline/11-Journal |

---

## 二、验收自检（对照 验收文档一票否决项）

| 项 | 状态 |
|----|------|
| `getPlayerToken()` 不再本地自造 token | ✅ 本轮修复 |
| join/restore-session 返回服务端 token | ✅ 后端已有 |
| stateVersion 屏障落地（后端） | ✅ 13-State/15-Projection |
| reconnect 安全过滤 | ✅ 10-Timeline |
| 玩家端不消费 Host-only/他人 private | ✅ 后端过滤 |
| 地图无 token → 401/403 | ✅ 09-Map |
| 线索分享不靠前端硬改 | ✅ 08-Clue |
| 401/403/404/409/429/500 有恢复路径 | ⚠️ 部分（前端状态机未全覆盖） |

---

## 三、已知遗留

| 项 | 说明 |
|----|------|
| `localStorage` token | 仍保留（开发态便利），生产应切到 secure session |
| WS query token 脱敏 | 未做 |
| 短期 WS ticket | 未引入 |
| 前端 `stateVersion` 屏障 | 后端已完备，前端未实现四类测试（旧丢弃/当前应用/未来buffer/重放） |
| `clue_id/id` 适配 | 前端未统一 |

---

## 四、测试基线

| 指标 | 数值 |
|------|------|
| 玩家端后端测试 | **58 passed** (8 xlsx errors, unchanged) |
| 前端构建 | ✅ passed (895ms) |

---

## 五、结论

### 本轮工程验收：✅ 通过

P0 安全边界已由后端 batch (05/08/09/10/13/15) 闭环。本轮修复了 `getPlayerToken()` 本地自造 token 的关键风险。

### 生产安全完成：⚠️ 未达到

`localStorage` token、WS query token 脱敏、短期 ticket、前端 stateVersion 四类测试、clue_id 适配等前端遗留项未收口。
