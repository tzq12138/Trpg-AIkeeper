# Channel 聊天与信息流系统 — 文档更新回执

**日期:** 2026-07-06
**审查范围:** `功能列表.md` / `PRD.md` / `DeepSeek计划.md` 最新版本
**实现基线:** Channel-0 至 Channel-5 全部批次已完成

---

## 一、文档变化概要

更新版新增约 +30% 内容，集中在三块：**统一可见性过滤方向**（要求各入口复用同一 helper）、**事件分类与 AI 事实层**（8 种事件类型的消费策略）、**P0 风险细化**（reconnect 全量、events/public host 泄漏、system_import）。

---

## 二、逐项审查结论

### ✅ 已闭环

| 批次 | 内容 | 测试 |
|------|------|------|
| Channel-0 | 事件类型从 registry 动态读取 | `test_events.py` (3 tests) |
| Channel-1 | WS catch-up `get_events_for_player` 过滤 | `test_channel_security.py` (4 tests) |
| Channel-2 | team-message 校验 (长度/白名单/限频) | `test_channel_messages.py` (6 tests) |
| Channel-3 | archive messages 类型 + HostLogsPanel 过滤 | `test_archive.py` |
| Channel-4 | `PlayerChatMessage.sender: 'team'` | types.ts |

### ⚡ 本次补充 (P0 缺口)

| # | 问题 | 修复 | 文件 |
|---|------|------|------|
| 1 | `/events/public` 泄 host 事件 | `audience != 'player'` → `audience IN ('party', 'system')` | `event_log.py:85` |
| 2 | player 可用 `system_import` | 从 `ALLOWED_SOURCES` 移除 | `router_player.py:438` |
| 3 | 测试覆盖 host 泄漏 | 新增 host-audience 排除断言 | `test_event_log.py:62-65` |

### 📋 演进方向

| # | 项目 | 优先级 | 备注 |
|---|------|--------|------|
| 1 | 统一可见性过滤 helper | P1 | WS/reconnect/archive/events/public 共享 |
| 2 | `characterId/character_id` 键名统一 | P1 | 过滤器只覆盖一半 payload 形态 |
| 3 | archive 迁移到 sequence 游标 | P1 | 当前仍用 offset/limit |
| 4 | 超长文本 413 而非 400 | P1 | 语义更准确 |
| 5 | 正式频道模型表 | P1 | channel_id/type/audience/mode |
| 6 | OOC/IC 分层 | P1 | mode=ooc 标记 |

---

## 三、安全边界对照

| 风险 | 之前状态 | 当前状态 |
|------|---------|---------|
| WS catch-up 无过滤 → 泄漏 host/他人私密 | ✅ 已关闭 | `get_events_for_player()` |
| `/events/public` 泄漏 host 事件 | ⚡ 本次关闭 | `audience IN ('party', 'system')` |
| player 可用 system_import source | ⚡ 本次关闭 | 仅 server/internal |
| reconnect snapshot 全量返回 | 📋 未关闭 | P1 — 需复用 helper |
| 统一过滤口径各入口不一致 | 📋 未关闭 | P1 |
| team-message 无校验 | ✅ 已关闭 | 长度/白名单/限频 |

---

## 四、测试基线

```powershell
pytest tests/ -q
```

| 指标 | 数值 |
|------|------|
| 测试通过 | 409 |
| Channel 相关 | 31 |
| 错误 (xlsx) | 12 |
| 前端构建 | ✅ |

---

## 五、建议后续行动

1. **P1** — 抽象统一可见性过滤 helper，覆盖 WS/reconnect/archive/events/public/export
2. **P1** — reconnect snapshot 使用过滤查询
3. **P1** — archive 迁移到 sequence 游标翻页
4. **P2** — `characterId/character_id` 键名统一为 `character_id`
