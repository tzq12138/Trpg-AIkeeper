# Codebase imports FastMCP from mcp.server.fastmcp (v1 API); mcp 2.x renamed it
# to MCPServer and removed the fastmcp module. Pin <2 to keep the v1 import path.
mcp>=1.0.0,<2
httpx>=0.27.0
pydantic>=2.0.0
# KpBrain uses AsyncOpenAI directly (kp_brain.py top-level import)
openai>=1.0.0
